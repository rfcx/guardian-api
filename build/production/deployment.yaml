kind: Deployment
apiVersion: apps/v1
metadata:
  name: ranger-api
  namespace: production
spec:
  selector:
    matchLabels:
      app: ranger-api
  template:
    metadata:
      labels:
        app: ranger-api
    spec:
      containers:
        - name: ranger-api
          image: 887044485231.dkr.ecr.eu-west-1.amazonaws.com/ranger-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "512Mi"
              cpu: "1"
            limits:
              memory: "2000Mi"
              cpu: "2"
          envFrom:
            - secretRef:
                name: ranger-api-secrets
            - configMapRef:
                name: ranger-api-config
